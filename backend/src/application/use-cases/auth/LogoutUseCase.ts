/**
 * Use case per il logout dell'utente
 * Gestisce la logica di business per la disconnessione
 */
export class LogoutUseCase {
  constructor() {}

  async execute(): Promise<void> {
    return;
  }
}
