/**
 * Re-exports the shared questionnaire types.
 * Kept for backward compatibility — all existing imports continue to work.
 */
export type {
  QuestionType,
  QuestionOption,
  Question,
  Questionnaire,
} from '../../shared/hitl/questionnaire.types';
